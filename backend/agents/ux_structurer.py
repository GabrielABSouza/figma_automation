"""UX Structuring Agent — defines layout hierarchy and section priorities."""

from backend.orchestrator.state import PipelineState


async def structure_ux(state: PipelineState) -> PipelineState:
    """Define layout hierarchy per screen with section priorities."""
    # TODO: implement with LLM call
    return state
